import React from 'react';
import Background from '@/components/background';  // Pfad zu deiner Background-Komponente

const TestPage = () => {
    return (
        <div>
            <Background />
        </div>
    );
};

export default TestPage;
